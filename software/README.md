This code relies on [kmk](https://kmkfw.io/) and the adafruit display
libraries from the [adafruit circuit python bundle](https://circuitpython.org/libraries).

Just upload this code to a board running CircuitPython as `code.py` and it
should work.
